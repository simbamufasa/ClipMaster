<Window x:Class="ClipMaster.TrayMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        Deactivated="Window_Deactivated">

  <Border Background="#222222"
          BorderBrush="#353535"
          BorderThickness="1"
          CornerRadius="10"
          Padding="4,6"
          MinWidth="180">
    <Border.Effect>
      <DropShadowEffect Color="Black" Opacity="0.4"
                         BlurRadius="16" ShadowDepth="4"/>
    </Border.Effect>

    <StackPanel>
      <!-- Show ClipMaster -->
      <Border x:Name="ShowItem"
              Background="Transparent"
              CornerRadius="6"
              Padding="14,9"
              Margin="2,0"
              Cursor="Hand"
              MouseLeftButtonUp="ShowItem_Click"
              MouseEnter="Item_MouseEnter"
              MouseLeave="Item_MouseLeave">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE8A7;" FontFamily="Segoe MDL2 Assets"
                     FontSize="13" Foreground="#8e8e8e"
                     VerticalAlignment="Center" Margin="0,0,10,0"/>
          <TextBlock Text="Show ClipMaster"
                     FontFamily="Segoe UI Variable, Segoe UI"
                     FontSize="12.5" FontWeight="Medium"
                     Foreground="#e0e0e0"
                     VerticalAlignment="Center"/>
        </StackPanel>
      </Border>

      <!-- Separator -->
      <Border Height="1" Background="#333333" Margin="10,4"/>

      <!-- Export backup -->
      <Border x:Name="ExportItem"
              Background="Transparent"
              CornerRadius="6"
              Padding="14,9"
              Margin="2,0"
              Cursor="Hand"
              MouseLeftButtonUp="ExportItem_Click"
              MouseEnter="Item_MouseEnter"
              MouseLeave="Item_MouseLeave">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE792;" FontFamily="Segoe MDL2 Assets"
                     FontSize="13" Foreground="#8e8e8e"
                     VerticalAlignment="Center" Margin="0,0,10,0"/>
          <TextBlock Text="Export backup…"
                     FontFamily="Segoe UI Variable, Segoe UI"
                     FontSize="12.5" FontWeight="Medium"
                     Foreground="#e0e0e0"
                     VerticalAlignment="Center"/>
        </StackPanel>
      </Border>

      <!-- Separator -->
      <Border Height="1" Background="#333333" Margin="10,4"/>

      <!-- Quit -->
      <Border x:Name="QuitItem"
              Background="Transparent"
              CornerRadius="6"
              Padding="14,9"
              Margin="2,0"
              Cursor="Hand"
              MouseLeftButtonUp="QuitItem_Click"
              MouseEnter="Item_MouseEnter"
              MouseLeave="Item_MouseLeave">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets"
                     FontSize="13" Foreground="#8e8e8e"
                     VerticalAlignment="Center" Margin="0,0,10,0"/>
          <TextBlock Text="Quit"
                     FontFamily="Segoe UI Variable, Segoe UI"
                     FontSize="12.5" FontWeight="Medium"
                     Foreground="#e05252"
                     VerticalAlignment="Center"/>
        </StackPanel>
      </Border>
    </StackPanel>
  </Border>
</Window>
